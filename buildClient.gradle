plugins{
    id 'java'
    id 'application'
    id 'com.github.ben-manes.versions' version '0.51.0' //replaces the old versions of dependencies
    id 'se.patrikerdes.use-latest-versions' version '0.2.18'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}

apply from : 'common.gradle'

application {
    mainClassName = 'main.CSLIDSMainClient'
}
